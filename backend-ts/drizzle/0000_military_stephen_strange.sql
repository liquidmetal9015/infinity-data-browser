-- Baseline: schema was previously managed by Alembic (see backend/alembic/versions/).
-- This file is intentionally a no-op — running it on a DB created by Alembic
-- captures the current state as the Drizzle starting point so subsequent
-- migrations apply cleanly.
SELECT 1;
